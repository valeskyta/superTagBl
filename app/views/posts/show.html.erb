

<div class="post">


<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
</div>

<div class="comments post_<%= @post.id %>">
	<%= render partial: "comments/comment", collection: @last_comments %>
</div>
<%= render "comments/form" %>

</div>